# 2026-09-13

## Unity 场景对象重命名后的代码同步
- 场景 `Assets/Scenes/Rigid Body Interaction.unity` 中：Cube → `Link`（连杆，带 Rigidbody+BoxCollider，Constraints=94），Sphere → `EnvObstacle`（环境障碍物，带 Rigidbody+SphereCollider）。
- 代码里唯一依赖对象名的地方：`SerialController.cs` 的 `GameObject.Find("envCube")`（旧名，实际早就匹配不上）。
- 已修改：
  - `SerialController.cs`：字段 `envCube` → `envObstacle`（加 `[FormerlySerializedAs("envCube")]` 防止 Inspector 引用丢失），Find 字符串改为 `"EnvObstacle"`，找不到时增加 `Debug.LogWarning`，注释同步。
  - `InteractWithVirtual.cs`：Tooltip/注释里 "Cube" → "Link"。
- 注意：`envObstacle` 目前只被赋值、未被任何逻辑使用（保留备用）；`targetCube` 为空时默认取脚本自身 transform。脚本当前未挂到场景中（场景文件里没有对应 MonoBehaviour 块）。
- ESP32 侧 (`ESP32code/src/main.cpp`) 不涉及 Unity 对象名，无需改。

## 编写项目 README
- 为项目根目录创建 `README.md`：系统架构（ESP32 SimpleFOC 力反馈 + Unity 2022.3.62f3c1 上位机）、串口协议（`S` 握手 / `F<value>` 力矩指令 / 角度逐行上报，115200 8N1）、硬件清单（LOLIN32 Lite、极对数 7 的 BLDC、AS5600 I2C 于 19/18 脚）、目录结构、快速开始与调参指南。
- 关键架构事实：位置差力矩计算在 Unity 侧（`InteractWithVirtual.cs`，Kp=4），MCU 只执行力矩并叠加 0.05*shaft_velocity 阻尼；无碰撞时虚拟 Link 用 SmoothDampAngle 视觉跟随，碰撞时切力反馈；fixedDeltaTime=1/250s。

## README 加入演示视频
- 视频源 `D:\ProgramData\QQdata\lv_0_20260913204357.mp4`（1080×1920 竖屏，45 s，HEVC，53 MB）→ 复制到 `assets/demo.mp4`；另压出 `assets/demo_small.mp4`（960×1706，7.1 MB，H.264 baseline/main + faststart，无音轨）与封面 `assets/cover.jpg`。
- README 新增「效果演示」章节：`<video>` 标签（poster=cover.jpg）+ 文件清单表 + GitHub 内联播放的折叠说明（GitHub 过滤 `<video>`，需把 mp4 拖进 Issue/PR 换 `github.com/user-attachments/assets/<id>.mp4` 链接）。
- `.gitattributes` 增加 `*.mp4/mov/png/jpg binary`，否则 `* text=auto` 的 LF 规范化会损坏二进制。
- 环境备忘：本机无 ffmpeg；用 `C:\Users\ASUS\.workbuddy\binaries\python\envs\default\Scripts\python.exe -m pip install imageio-ffmpeg` 后由 `imageio_ffmpeg.get_ffmpeg_exe()` 取得可执行文件（含 libx264）。
- 环境备忘：本会话 Bash shim 异常（`dirname/tail/ls` 找不到），优先用 node.exe -e 或 venv 里的 python.exe 跑脚本，避免依赖 PATH 上的 coreutils。
- 环境备忘：ffmpeg 在 `C:\Users\ASUS\AppData\Local\kzip_sogou\ffmpeg.exe`（含 ffprobe，可用）。

## README 演示视频改为 GitHub 页面内嵌可播放（第二版）
- 结论：GitHub 的 README 渲染器只把 **`https://github.com/user-attachments/assets/<uuid>` 这种上传链接**（裸链接，不带任何 markdown/HTML 包装）渲染成可播放播放器；仓库内的 `.mp4`（raw 链接）、`<video>` 标签一律被过滤/降级成下载链接。因此"直接看到播放器"必须靠网页端拖拽上传换取的 user-attachments 链接，无法由本地代码自动完成。
- `assets/demo_720.mp4` 新增：为上传而做的压缩版（720×1278，12 fps，CRF 30，无音轨，faststart，1.8 MB，远低于免费账户 10 MB 上限）。压缩命令：`ffmpeg -i demo_small.mp4 -vf "scale=720:-2,fps=12" -c:v libx264 -profile:v main -pix_fmt yuv420p -crf 30 -an -movflags +faststart demo_720.mp4`。
- README「效果演示」章节重写：第 7 行一个占位 user-attachments 链接（待网页端上传后替换）+ 一行说明；折叠块内保留 cover.jpg 点击链接到 demo_small.mp4 的兜底入口与文件体积说明。
- 仓库远端 `origin` = https://github.com/Little-Seals/VR_Device_Demo.git（分支 main），git-lfs 3.5.1 已可用；已 commit+push（ee36eae）。
